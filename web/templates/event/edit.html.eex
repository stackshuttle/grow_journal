<h2>Edit event</h2>

<%= render "form.html", changeset: @changeset,
                        action: plant_event_path(@conn, :update, @plant, @event) %>

<%= link "Back", to: plant_path(@conn, :index) %>
